apiVersion: v1
kind: Service
metadata:
  name: home-library-service
  namespace: default
spec:
  selector:
    app: home-library
  type: LoadBalancer
  ports:
    - name: web
      protocol: TCP
      port: 80
      targetPort: 3000
  loadBalancerIP: 192.168.1.31
